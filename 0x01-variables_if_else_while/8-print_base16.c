#include <stdio.h>
#include <unistd.h>

/**
 * main - Product entry point.
 * Description: Print base 16 numbers.
 * Return: 0/nothing.
 */

int main(void)

{
char i;
char j = '0';
	while (j <= '9')
	{
		putchar(j);								
	d++;
}
	for (i = 'a'; i <= 'f'; i++)
{
	putchar(i);
}
	putchar('\n');
return (0);
}
