#include "main.h"
#include <stdio.h>

/**
 * print_numbers - function entry point.
 *Return: 0/nothing is returned.
 */
void print_numbers(void)
{
	for (int n = 48; i <= 57; i++)
	{
		putchar(i);
	}
		putchar(10);
}
