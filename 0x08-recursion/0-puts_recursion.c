#include "main.h"
#include <stdio.h>

/**
 * _puts_recursion - function to print to the terminal using put.
 * Description - printing string of text using puts.
 * @s : pointer to string of text to print.
 */

void _puts_recursion(char *s)
{
	puts(s);
}
